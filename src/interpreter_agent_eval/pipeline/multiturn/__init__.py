"""Multi-turn two-party conversation support (docs/multiturn_expansion_plan.md).

Additive subpackage: nothing here is imported by the single-turn pipeline
(``pipeline/operations.py`` / ``pipeline/stages.py``), and this subpackage may
freely import from there (read-only) without creating a cycle.
"""
